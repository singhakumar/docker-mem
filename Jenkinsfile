pipeline {
    agent { label 'docker-node' }
#   agent { dockerfile true }
    stages {
        stage('Git Pull') {
            steps {
                sh 'echo hello'
            }
        }
    }
}
